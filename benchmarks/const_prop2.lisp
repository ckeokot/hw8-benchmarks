(define (test)
  (let ((a 10))
    (let ((b 20))
      (if (< a b)
          (let ((c (+ a b)))
            (if (< c 0)
                (+ a (+ b c))
                (- a (- b c))))
          (let ((d (- b a)))
            (if (= d 0)
                (+ a b)
                (- b a)))))))

(test) 